apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ncpollbook.fullname" . }}-test-connection"
  labels:
    {{- include "ncpollbook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ncpollbook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
